---
phase: 02-community-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/PULL_REQUEST_TEMPLATE/skill-submission.md
  - .github/PULL_REQUEST_TEMPLATE/dev-to-main.md
  - .gitignore
autonomous: true
requirements: [COM-01, COM-02, COM-03, COM-04, COM-05]

must_haves:
  truths:
    - "Public repo LICENSE is detected as MIT by GitHub SPDX"
    - "Public repo DISCLAIMER.md exists at root"
    - "Public repo issue templates submit-skill.yml and submit-plugin.yml are functional"
    - "Public repo PR template skill-submission.md has no broken references"
    - "Public repo PR template dev-to-main.md exists with compliance checklist"
    - "Public repo .gitignore excludes node_modules, .env, dist, .internal/, CLAUDE.local.md, .planning/"
  artifacts:
    - path: ".github/PULL_REQUEST_TEMPLATE/skill-submission.md"
      provides: "Updated PR template without broken references to docs/schema.md or scripts/validate-submission.js"
      contains: "content.config.ts"
    - path: ".github/PULL_REQUEST_TEMPLATE/dev-to-main.md"
      provides: "Release compliance checklist for dev-to-main PRs"
      contains: "physician-review-approved"
    - path: ".gitignore"
      provides: "Git ignore rules for public repo"
      contains: ".planning/"
  key_links:
    - from: ".github/PULL_REQUEST_TEMPLATE/skill-submission.md"
      to: "src/content.config.ts"
      via: "reference link in checklist"
      pattern: "content.config.ts"
    - from: ".github/ISSUE_TEMPLATE/submit-skill.yml"
      to: "src/content.config.ts"
      via: "category dropdown matching Zod schema"
      pattern: "diagnosis|treatment|lab-imaging"
---

<objective>
Push community standards files to the public repo Open-Medica/open-medical-skills: fix PR template broken references, add dev-to-main PR template, create .gitignore, and verify all 5 COM requirements.

Purpose: The public repo must meet GitHub community standards before external contributors arrive. LICENSE, DISCLAIMER.md, and issue templates already exist from Phase 1. This plan fixes the PR template references, adds the missing dev-to-main template and .gitignore, and pushes everything to the public repo.

Output: All community standard files verified on the public repo. COM-01 through COM-05 complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-public-repository-creation/01-01-SUMMARY.md

<interfaces>
<!-- Files on the public repo that already exist and are correct (from Phase 1): -->
<!-- LICENSE: Pure MIT text, copyright IntelMedica.ai 2026, SPDX detected as MIT -->
<!-- DISCLAIMER.md: Medical research disclaimer, complete, no changes needed -->
<!-- .github/ISSUE_TEMPLATE/submit-skill.yml: 256 lines, 14-category dropdown, attestation checkboxes -->
<!-- .github/ISSUE_TEMPLATE/submit-plugin.yml: 333 lines, 14-category dropdown, attestation checkboxes -->
<!-- .github/PULL_REQUEST_TEMPLATE/skill-submission.md: EXISTS but has 2 broken references -->
<!-- .github/PULL_REQUEST_TEMPLATE/dev-to-main.md: DOES NOT EXIST on public repo -->
<!-- .gitignore: DOES NOT EXIST on public repo -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix skill-submission PR template and create .gitignore for public repo</name>
  <files>.github/PULL_REQUEST_TEMPLATE/skill-submission.md, .gitignore</files>
  <read_first>
    - .github/PULL_REQUEST_TEMPLATE/skill-submission.md
    - .github/PULL_REQUEST_TEMPLATE/dev-to-main.md
    - .gitignore
  </read_first>
  <action>
**1. Update `.github/PULL_REQUEST_TEMPLATE/skill-submission.md`** (per D-08, D-09):

Replace line 14 which currently reads:
```
- [ ] The YAML file follows the [schema specification](../../docs/schema.md)
```
with:
```
- [ ] The YAML file follows the [content schema](../../src/content.config.ts)
```

Remove line 19 which currently reads:
```
- [ ] The YAML file passes local validation (`node scripts/validate-submission.js`)
```
Replace it with:
```
- [ ] CI validation passes on this PR (automated — no local step needed)
```

All other content in skill-submission.md stays exactly as-is. Do not change any other lines.

**2. Create `.gitignore` for the public repo** (per D-11, D-12, D-13):

Create a new `.gitignore` file in the project root with these exact contents:

```
# Dependencies
node_modules/
.pnp/
.pnp.js

# Build output
dist/
.astro/
out/
build/

# Environment
.env
.env.local
.env.production
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Development-only (GSD workflow artifacts)
.planning/

# Private infrastructure (safety net against accidental commits)
.internal/
.agents/
AGENTS/
.claude/
CLAUDE.local.md
AGENTS.md
GEMINI.md
instructions.md
.mcp.json
GUARDRAILS.md
refs/
data/
wrangler.local.toml

# Handoff (session-specific)
HANDOFF.md

# Temp build artifacts
_astro-temp/

# Research docs
SKILLS-RESEARCH.md
```

Note: This .gitignore is for the PUBLIC repo. It is intentionally a subset — no private-repo-specific paths (workers/, src/, etc.) because those don't exist in the public repo. The `.planning/` entry (D-11) prevents GSD artifacts from being committed. The private infrastructure paths (D-12) are a safety net. The agent/config paths (D-13) prevent orchestration files from leaking.
  </action>
  <verify>
    <automated>grep -q "content.config.ts" .github/PULL_REQUEST_TEMPLATE/skill-submission.md && grep -q "CI validation passes" .github/PULL_REQUEST_TEMPLATE/skill-submission.md && ! grep -q "docs/schema.md" .github/PULL_REQUEST_TEMPLATE/skill-submission.md && ! grep -q "validate-submission.js" .github/PULL_REQUEST_TEMPLATE/skill-submission.md && grep -q ".planning/" .gitignore && grep -q "node_modules/" .gitignore && grep -q "CLAUDE.local.md" .gitignore && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/PULL_REQUEST_TEMPLATE/skill-submission.md` contains `[content schema](../../src/content.config.ts)` (not `docs/schema.md`)
    - `.github/PULL_REQUEST_TEMPLATE/skill-submission.md` contains `CI validation passes on this PR` (not `validate-submission.js`)
    - `.github/PULL_REQUEST_TEMPLATE/skill-submission.md` does NOT contain `docs/schema.md`
    - `.github/PULL_REQUEST_TEMPLATE/skill-submission.md` does NOT contain `validate-submission.js`
    - `.gitignore` contains `node_modules/`
    - `.gitignore` contains `.env`
    - `.gitignore` contains `dist/`
    - `.gitignore` contains `.internal/`
    - `.gitignore` contains `CLAUDE.local.md`
    - `.gitignore` contains `.planning/`
    - `.gitignore` contains `GUARDRAILS.md`
    - `.gitignore` contains `.mcp.json`
  </acceptance_criteria>
  <done>skill-submission.md has no broken references (D-08, D-09 implemented), .gitignore exists with all required exclusions (D-11, D-12, D-13 implemented)</done>
</task>

<task type="auto">
  <name>Task 2: Push all community standards files to public repo and verify all COM requirements</name>
  <files>Remote: Open-Medica/open-medical-skills (push via git)</files>
  <read_first>
    - .github/PULL_REQUEST_TEMPLATE/skill-submission.md
    - .github/PULL_REQUEST_TEMPLATE/dev-to-main.md
    - .gitignore
    - DISCLAIMER.md
  </read_first>
  <action>
This task pushes the updated/new files to the public repo `Open-Medica/open-medical-skills` and verifies all 5 COM requirements.

**Push process** (same pattern as Phase 1 -- clone, copy, commit, push):

1. Clone the public repo to a temp directory:
   ```bash
   git clone https://github.com/Open-Medica/open-medical-skills.git /tmp/oms-public-02
   cd /tmp/oms-public-02
   ```

2. Copy the 3 files that need updating/creating from the private repo working directory:
   ```bash
   # Updated PR template (D-08, D-09 fixes)
   cp /home/jfmd/.jfmd/projects/INTELMEDICA-COMP/open-medical-skills/.github/PULL_REQUEST_TEMPLATE/skill-submission.md .github/PULL_REQUEST_TEMPLATE/skill-submission.md

   # New PR template (dev-to-main compliance checklist)
   cp /home/jfmd/.jfmd/projects/INTELMEDICA-COMP/open-medical-skills/.github/PULL_REQUEST_TEMPLATE/dev-to-main.md .github/PULL_REQUEST_TEMPLATE/dev-to-main.md

   # New .gitignore
   cp /home/jfmd/.jfmd/projects/INTELMEDICA-COMP/open-medical-skills/.gitignore .gitignore
   ```

   Note: Issue templates (submit-skill.yml, submit-plugin.yml) already exist on the public repo and are correct per D-05 -- do NOT re-push them unless they differ. LICENSE and DISCLAIMER.md also already exist from Phase 1.

3. Commit and push to main:
   ```bash
   git add .github/PULL_REQUEST_TEMPLATE/skill-submission.md .github/PULL_REQUEST_TEMPLATE/dev-to-main.md .gitignore
   git commit -m "feat: add community standards (PR templates, .gitignore)"
   git push origin main
   ```

4. Update dev branch to include the new commit:
   ```bash
   git checkout dev
   git merge main --ff-only
   git push origin dev
   ```

**Verification of all 5 COM requirements via GitHub API:**

- **COM-01** (LICENSE MIT): `gh api repos/Open-Medica/open-medical-skills/license --jq '.license.spdx_id'` must return `MIT`
- **COM-02** (DISCLAIMER.md): `gh api repos/Open-Medica/open-medical-skills/contents/DISCLAIMER.md --jq '.name'` must return `DISCLAIMER.md`
- **COM-03** (Issue templates): `gh api repos/Open-Medica/open-medical-skills/contents/.github/ISSUE_TEMPLATE --jq '.[].name'` must show `submit-skill.yml` and `submit-plugin.yml`
- **COM-04** (PR templates): `gh api repos/Open-Medica/open-medical-skills/contents/.github/PULL_REQUEST_TEMPLATE --jq '.[].name'` must show `skill-submission.md` and `dev-to-main.md`
- **COM-05** (.gitignore): `gh api repos/Open-Medica/open-medical-skills/contents/.gitignore --jq '.name'` must return `.gitignore`

5. Clean up: `rm -rf /tmp/oms-public-02`
  </action>
  <verify>
    <automated>gh api repos/Open-Medica/open-medical-skills/license --jq '.license.spdx_id' 2>/dev/null | grep -q "MIT" && gh api repos/Open-Medica/open-medical-skills/contents/DISCLAIMER.md --jq '.name' 2>/dev/null | grep -q "DISCLAIMER.md" && gh api repos/Open-Medica/open-medical-skills/contents/.github/ISSUE_TEMPLATE --jq '.[].name' 2>/dev/null | grep -q "submit-skill.yml" && gh api repos/Open-Medica/open-medical-skills/contents/.github/PULL_REQUEST_TEMPLATE --jq '.[].name' 2>/dev/null | grep -q "dev-to-main.md" && gh api repos/Open-Medica/open-medical-skills/contents/.gitignore --jq '.name' 2>/dev/null | grep -q ".gitignore" && echo "ALL COM REQUIREMENTS PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/Open-Medica/open-medical-skills/license --jq '.license.spdx_id'` returns `MIT` (COM-01)
    - `gh api repos/Open-Medica/open-medical-skills/contents/DISCLAIMER.md --jq '.name'` returns `DISCLAIMER.md` (COM-02)
    - `gh api repos/Open-Medica/open-medical-skills/contents/.github/ISSUE_TEMPLATE --jq '.[].name'` includes `submit-skill.yml` and `submit-plugin.yml` (COM-03)
    - `gh api repos/Open-Medica/open-medical-skills/contents/.github/PULL_REQUEST_TEMPLATE --jq '.[].name'` includes `skill-submission.md` and `dev-to-main.md` (COM-04)
    - `gh api repos/Open-Medica/open-medical-skills/contents/.gitignore --jq '.name'` returns `.gitignore` (COM-05)
    - Public repo `dev` branch is fast-forward merged with `main` (both branches have the new commit)
  </acceptance_criteria>
  <done>All 5 COM requirements verified on the public repo via GitHub API. Both main and dev branches contain the community standards files.</done>
</task>

</tasks>

<verification>
Run all 5 COM requirement checks against the public GitHub API:

1. COM-01: `gh api repos/Open-Medica/open-medical-skills/license --jq '.license.spdx_id'` returns `MIT`
2. COM-02: `gh api repos/Open-Medica/open-medical-skills/contents/DISCLAIMER.md --jq '.name'` returns `DISCLAIMER.md`
3. COM-03: `gh api repos/Open-Medica/open-medical-skills/contents/.github/ISSUE_TEMPLATE --jq '.[].name'` shows both templates
4. COM-04: `gh api repos/Open-Medica/open-medical-skills/contents/.github/PULL_REQUEST_TEMPLATE --jq '.[].name'` shows both templates
5. COM-05: `gh api repos/Open-Medica/open-medical-skills/contents/.gitignore --jq '.name'` returns `.gitignore`

Additionally verify skill-submission.md content on remote:
6. `gh api repos/Open-Medica/open-medical-skills/contents/.github/PULL_REQUEST_TEMPLATE/skill-submission.md --jq '.content' | base64 -d | grep -c 'content.config.ts'` returns >= 1
7. `gh api repos/Open-Medica/open-medical-skills/contents/.github/PULL_REQUEST_TEMPLATE/skill-submission.md --jq '.content' | base64 -d | grep -c 'docs/schema.md'` returns 0
</verification>

<success_criteria>
- All 5 COM requirements pass GitHub API verification
- skill-submission.md references content.config.ts (not docs/schema.md)
- skill-submission.md references CI validation (not validate-submission.js)
- dev-to-main.md exists with physician-review-approved checklist item
- .gitignore includes .planning/, .internal/, CLAUDE.local.md, node_modules/, .env, dist/
- Public repo main and dev branches are in sync
</success_criteria>

<output>
After completion, create `.planning/phases/02-community-standards/02-01-SUMMARY.md`
</output>
